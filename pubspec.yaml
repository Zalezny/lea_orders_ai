name: lea_orders_ai
description: "Recruitment project to Leaware - orders management app with AI features"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  retrofit: ^4.8.0
  logger: ^2.6.2
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  injectable: ^2.5.2
  get_it: ^8.3.0
  flutter_bloc: ^9.1.1
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.10.1
  injectable_generator: ^2.9.0
  freezed: ^3.2.3
  json_serializable: ^6.11.1
  retrofit_generator: ^10.0.6

flutter:
  uses-material-design: true


